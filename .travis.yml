language: java
branches:
  only:
  - develop 

scripts:
        - mvn flyway:migrate
        - mvn test
        - rm csci310.db
        - mvn flyway:migrate
        - mvn jetty:run &
        - sleep 30
        - mvn -DrunCucumberTests=true -Dmobile=true test
